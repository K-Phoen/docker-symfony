tools:
  build: ./docker/buildtools
  working_dir: /srv/app
  volumes:
    - ./code:/srv/app

web:
  build: ./docker/web
  working_dir: /srv/app
  ports:
    - "8080:80"
  links:
    - db
  volumes:
    - ./code:/srv/app

db:
  image: orchardup/mysql
  environment:
    MYSQL_DATABASE: wordpress
